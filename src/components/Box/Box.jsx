import styled from 'styled-components';
import {
  layout,
  space,
  flexbox,
  color,
  borders,
  shadow,
  position,
} from 'styled-system';

export const Box = styled.div`
  ${layout}
  ${flexbox}
  ${space}
  ${position}
  ${color}
  ${borders}
  ${shadow}
`;
